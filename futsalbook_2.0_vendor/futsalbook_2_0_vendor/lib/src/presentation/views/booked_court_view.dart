import 'package:flutter/material.dart';

class BookedCourtPage extends StatelessWidget {
  const BookedCourtPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Booked court list"),
      ),
    );
  }
}
