import 'package:flutter/material.dart';

class CourtListPage extends StatelessWidget {
  const CourtListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Courts List Page"),
      ),
    );
  }
}
