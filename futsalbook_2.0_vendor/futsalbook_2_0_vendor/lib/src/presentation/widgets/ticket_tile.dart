import 'package:flutter/material.dart';

class BookedCourtTicket extends StatelessWidget {
  const BookedCourtTicket({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      mainAxisAlignment: MainAxisAlignment.start,
      children: [],
    );
  }
}
